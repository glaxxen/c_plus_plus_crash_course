#include <iostream>

int main() {

  int medicalStudents = 30;
  // medicalStudents = medicalStudents + 20;
  // medicalStudents += 20; // add up any number.

  // medicalStudents -= 1;
  // medicalStudents *= 2;
  // medicalStudents /= 2;
  // medicalStudents %= 7; // modulus

  // increment operator (++) - ONLY WANT TO ADD BY 1
  // medicalStudents++;
  // increment operator (--) - ONLY WANT TO ADD BY 1
  // medicalStudents--;

  // * OPERATOR PRECEDENCE
    // 1. Parenthesis
    // 2. Multiplicatioon and Division
    // 3. Addition & Subtraction

  // int otherMedicalStudents = 6 - (5 + 4) * 3 / 2;

  // std::cout << otherMedicalStudents;


  return 0;
}